//Form where the inputs are all readonly

include includes/header.pug
div#page-container 
    include includes/nav
    div#content-container
        div(class="flex-row-container centre-flex-row-items" style="margin-top: var(--medium-px)")
            div(class="title-text-box blue-title-text-box" style="padding: var(--medium-px); margin: 0px var(--medium-px) 0px var(--medium-px); hyphens: none;").
                Employee Details
        div(class="scroll-flex-row-container centre-flex-row-items" style="margin-top: var(--medium-px)")
            form(class="vertical-form")
                div(style="height: 320px")
                    label(for="firstName") First Name 
                    label(for="lastName") Last Name
                    label(for="address") Address
                    label(for="phoneNumber") Phone Number 
                    label(for="dateOfBirth") Date of Birth 
                    label(for="emergencyPhoneNumber") Emergency Number
                div(style="width: 15vw; min-width: 150px; height: 270px")   
                    input(type="text" name="employeeFirstName" placeholder=`${passedInUser.firstName}` readonly)  
                    input(type="text" name="employeeLastName" placeholder=`${passedInUser.lastName}` readonly) 
                    input(type="text" name="employeeAddress" placeholder=`${passedInUser.address}` readonly) 
                    input(type="text" name="employeePhoneNumber" placeholder=`${passedInUser.phoneNumber}` readonly) 
                    input(type="text" name="employeeDateOfBirth" placeholder=`${passedInUser.dateOfBirth}` readonly) 
                    input(type="text" name="employeeEmergencyNumber" placeholder=`${passedInUser.emergencyNumber}` readonly)
                div 
                    a(class="primary-button" href="editDetails.html") Edit Details
                    a(class="secondary-button" href="employees.html") Home